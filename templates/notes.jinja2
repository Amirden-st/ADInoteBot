{% from "note.jinja2" import header, content, footer %}
{% for note in notes %}
{% if note is not string %}
{{ header(note.title, note.id) }}
{{ content(note.content, content_length) }}
{{ footer(show_date, note.created_at, note.category_name) }}
{% endif %}
{% endfor %}
{% set not_found = notes | selectattr("content", "undefined") | list %}
{% if not_found %}

||||||||||||||||||||||||||||||||
Not found notes:
{% for id in not_found %}{{ id }}{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
